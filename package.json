{
	"name": "stats-umami",
	"private": true,
	"version": "1.1.0",
	"description": "Editor JS build tooling for the Stats Umami WordPress plugin (dev-only; the shipped plugin has zero runtime JS dependencies).",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"test-unit-js": "wp-scripts test-unit-js"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.27.0"
	}
}
